name: Deploy to Server

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Позволяет запускать workflow вручную

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to server via SSH
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT }}
        script: |
          # Ищем директорию проекта
          echo "Поиск директории проекта..."
          find $HOME -name "bot_business_card" -type d 2>/dev/null || true
          find /root -name "bot_business_card" -type d 2>/dev/null || true
          find /home -name "bot_business_card" -type d 2>/dev/null || true
          
          # Если проекта нет, клонируем его
          if [ ! -d "$HOME/bot_business_card" ]; then
            echo "Проект не найден, клонируем репозиторий..."
            cd $HOME
            git clone https://github.com/deathdaycome/MYBOTTZ.git bot_business_card
          fi
          
          # Переходим в директорию проекта
          cd $HOME/bot_business_card
          
          # Проверяем что мы в правильной директории
          pwd
          ls -la
          
          # Сохраняем локальные изменения (если есть)
          git stash
          
          # Получаем последние изменения
          git pull origin main
          
          # Восстанавливаем локальные изменения
          git stash pop || true
          
          # Создаем виртуальное окружение если его нет
          if [ ! -d "venv" ]; then
            echo "Создаем виртуальное окружение..."
            python3 -m venv venv
          fi
          
          # Активируем виртуальное окружение
          source venv/bin/activate
          
          # Обновляем зависимости
          pip install -r requirements.txt
          
          # Очищаем кэш Python для применения изменений
          find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
          find . -type f -name "*.pyc" -delete 2>/dev/null || true
          
          # Выполняем миграции базы данных (если нужно)
          # python -m alembic upgrade head
          
          # Полностью останавливаем и удаляем процесс
          pm2 stop bot-business-card || true
          pm2 delete bot-business-card || true
          
          # Запускаем PM2 из текущей директории
          pm2 start ./venv/bin/python --name bot-business-card --interpreter none -- -m app.main
          
          # Сохраняем конфигурацию PM2
          pm2 save
          
          # Проверяем статус
          pm2 status
          
          # Показываем последние логи
          pm2 logs bot-business-card --lines 10 --nostream
          
          echo "✅ Деплой завершен успешно! Бот перезапущен через PM2"