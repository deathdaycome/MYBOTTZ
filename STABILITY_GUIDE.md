# 🛡️ Руководство по стабильности CRM системы

## 📋 Что было сделано

### 1. ✅ Исправлен Dockerfile
- Добавлена явная установка `websockets` и `wsproto`
- Гарантируется что WebSocket будет работать всегда

### 2. ✅ Система мониторинга и автовосстановления
**Файл:** `scripts/monitor_health.sh`

**Что делает:**
- Проверяет здоровье контейнеров каждую минуту
- Проверяет доступность API эндпоинтов
- Мониторит использование диска и памяти
- **Автоматически перезапускает** упавшие сервисы
- Отправляет уведомления в Telegram при проблемах

### 3. ✅ Автоматический backup базы данных
**Файл:** `scripts/backup_database.sh`

**Что делает:**
- Создает backup базы данных каждые 6 часов
- Сжимает backup (gzip)
- Хранит backup 30 дней
- Отправляет уведомления о результате
- Автоматически очищает старые backup

### 4. ✅ Скрипт установки
**Файл:** `scripts/install_monitoring.sh`

**Что делает:**
- Устанавливает все скрипты на сервер
- Создает systemd сервис для мониторинга
- Настраивает cron задачи для backup
- Запускает мониторинг автоматически

## 🚀 Установка

### Шаг 1: Загрузить файлы на сервер

```bash
# На локальной машине
scp Dockerfile root@147.45.215.199:/var/www/bot_business_card/
scp scripts/*.sh root@147.45.215.199:/var/www/bot_business_card/scripts/
```

### Шаг 2: Установить мониторинг

```bash
# На сервере
ssh root@147.45.215.199
cd /var/www/bot_business_card
chmod +x scripts/install_monitoring.sh
./scripts/install_monitoring.sh
```

### Шаг 3: Пересобрать контейнер

```bash
cd /var/www/bot_business_card
docker-compose build --no-cache
docker-compose up -d
```

## 📊 Проверка работы

### Проверить мониторинг
```bash
systemctl status crm-monitor
journalctl -u crm-monitor -f
```

### Проверить backup
```bash
ls -lh /var/www/bot_business_card/backups/database/
```

### Ручной backup
```bash
/var/www/bot_business_card/scripts/backup_database.sh
```

### Проверить cron задачи
```bash
crontab -l
```

## 🔧 Настройка уведомлений в Telegram

Добавьте в `.env`:

```bash
ADMIN_BOT_TOKEN=your_bot_token_here
ADMIN_CHAT_ID=your_chat_id_here
```

Теперь вы будете получать уведомления о:
- Проблемах с контейнерами
- Успешных backup
- Проблемах с диском/памятью
- Восстановлении сервисов

## 📈 Что это дает

### До:
- ❌ WebSocket отваливается после перезапуска контейнера
- ❌ Нет автоматического восстановления при сбоях
- ❌ Нет backup базы данных
- ❌ Нет мониторинга состояния
- ❌ Проблемы обнаруживаются только пользователями

### После:
- ✅ WebSocket всегда работает (встроено в образ)
- ✅ Автоматический перезапуск при сбоях (каждую минуту)
- ✅ Backup каждые 6 часов + ежедневный
- ✅ Постоянный мониторинг здоровья системы
- ✅ Проактивные уведомления о проблемах

## 🎯 Результат

Система теперь:
1. **Самовосстанавливается** - упавшие сервисы автоматически перезапускаются
2. **Защищена от потери данных** - регулярные backup
3. **Мониторится 24/7** - проблемы обнаруживаются мгновенно
4. **Стабильна** - WebSocket и все зависимости встроены в образ

## 📞 Поддержка

Если что-то пошло не так:

1. Проверить логи мониторинга: `journalctl -u crm-monitor -f`
2. Проверить логи контейнера: `docker logs bot-business-card`
3. Проверить статус: `docker ps -a`
4. Восстановить из backup: Распаковать `.gz` файл из `/var/www/bot_business_card/backups/database/`

## 🔄 Обновления в будущем

При следующих обновлениях системы просто:

```bash
# Пересоберите контейнер с новым кодом
docker-compose build --no-cache
docker-compose up -d

# Мониторинг и backup продолжат работать автоматически!
```
