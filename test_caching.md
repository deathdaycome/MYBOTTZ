# 🧪 Тест кеширования страницы Хостинга

## Как проверить что кеширование работает

### 1. Откройте DevTools (F12)

Перейдите во вкладку **Console**

### 2. Откройте страницу Хостинга первый раз

В консоли вы НЕ увидите сообщение о кеше (его еще нет)

Увидите примерно через 1-2 секунды:
```
💾 Saved to cache
```

### 3. Обновите страницу (F5)

В консоли вы СРАЗУ увидите:
```
📦 Loaded from cache (age: 5s)
```

Затем через ~100мс:
```
🔄 Background update completed
💾 Saved to cache
```

### 4. Подождите 1 минуту

В консоли автоматически появится:
```
⏰ Auto-refresh triggered
🔄 Background update completed
💾 Saved to cache
```

### 5. Проверьте индикатор времени

В правом верхнем углу должно быть:
```
Обновлено: 14:35:42  [Обновить] [Синхронизация с Timeweb] [+ Добавить сервер]
```

### 6. Проверьте localStorage

Во вкладке **Application** → **Local Storage** → выберите ваш домен

Должен быть ключ: `hosting_data_cache`

Кликните на него, увидите JSON с данными и timestamp.

### 7. Проверьте скорость загрузки

**Без кеша:**
1. Очистите localStorage: `localStorage.clear()`
2. Обновите страницу
3. Засеките время - должно быть ~1-2 секунды

**С кешем:**
1. Обновите страницу еще раз
2. Засеките время - должно быть ~0.1 секунды (мгновенно!)

## ✅ Ожидаемые результаты

- ✅ Первое открытие: загрузка 1-2 сек
- ✅ Повторное открытие: мгновенно (<0.1 сек)
- ✅ Данные в localStorage есть
- ✅ Индикатор "Обновлено" показывает время
- ✅ Автообновление каждую минуту
- ✅ Кнопка "Обновить" работает
- ✅ После изменений данные обновляются

## 🐛 Возможные проблемы

### Проблема: Не вижу сообщений в консоли

**Решение:** Убедитесь что открыта вкладка Console в DevTools

### Проблема: Страница все равно грузится долго

**Решение:**
1. Очистите кеш браузера (Ctrl+Shift+Delete)
2. Обновите страницу принудительно (Ctrl+F5)
3. Проверьте что сборка загружена на сервер

### Проблема: Данные не обновляются

**Решение:**
1. Нажмите кнопку "Обновить" вручную
2. Проверьте консоль на наличие ошибок
3. Проверьте что API доступно

### Проблема: localStorage переполнен

**Решение:**
```javascript
// В консоли браузера
localStorage.clear()
```

## 📊 Проверка производительности

### Измерение времени загрузки

```javascript
// В консоли браузера, перед обновлением страницы
performance.mark('page-start')

// После загрузки (когда видны данные)
performance.mark('page-end')
performance.measure('page-load', 'page-start', 'page-end')
console.log(performance.getEntriesByName('page-load'))
```

### Размер кеша

```javascript
// В консоли браузера
const cache = localStorage.getItem('hosting_data_cache')
console.log('Cache size:', (cache.length / 1024).toFixed(2), 'KB')
```

### Возраст кеша

```javascript
// В консоли браузера
const cache = JSON.parse(localStorage.getItem('hosting_data_cache'))
const age = (Date.now() - cache.timestamp) / 1000
console.log('Cache age:', age.toFixed(0), 'seconds')
```
