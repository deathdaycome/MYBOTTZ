# 🤖 Автоматическое присвоение Client ID

## 📋 Описание

Система автоматически присваивает уникальный `client_id` каждому серверу на основе **имени клиента**.

Серверы с **одинаковым именем клиента** получают **одинаковый** `client_id`, что позволяет:
- Автоматически группировать серверы одного клиента
- Рассчитывать общую стоимость всех серверов клиента
- Управлять единым балансом для всех серверов клиента

## 🎯 Как это работает

### 1. Автоматическое присвоение при создании/редактировании

Когда вы создаете или редактируете сервер:

```
1. Указываете имя клиента: "Иван Петров"
2. Система автоматически:
   - Проверяет, есть ли другие серверы с именем "Иван Петров"
   - Если есть - использует их client_id
   - Если нет - создает новый уникальный client_id
3. Присваивает client_id серверу
```

### 2. Группировка серверов

**Пример:**

```
Сервер 1: client_name = "Иван Петров" → client_id = 1234567
Сервер 2: client_name = "Иван Петров" → client_id = 1234567 (тот же!)
Сервер 3: client_name = "Мария Иванова" → client_id = 8901234 (новый)
```

Результат:
- Серверы 1 и 2 группируются под одним клиентом
- Баланс у них общий
- Месячная стоимость = сумма стоимостей обоих серверов

### 3. Автоматический расчет баланса

Система автоматически:
- Суммирует стоимость всех серверов клиента (`total_monthly_cost`)
- Рассчитывает дни на балансе: `дни = баланс / (стоимость_в_месяц / 30)`
- Обновляет эти значения при изменении серверов

## 💡 Практические примеры

### Пример 1: Новый клиент с несколькими серверами

**Шаг 1:** Создаете первый сервер
```
Имя сервера: "CRM система"
Имя клиента: "ООО Рога и Копыта"
Цена для клиента: 3000₽/мес
```
→ Система присваивает client_id = 1111111

**Шаг 2:** Создаете второй сервер для того же клиента
```
Имя сервера: "База данных"
Имя клиента: "ООО Рога и Копыта"  (то же имя!)
Цена для клиента: 2000₽/мес
```
→ Система находит существующий client_id = 1111111

**Результат:**
- Оба сервера имеют client_id = 1111111
- Месячная стоимость клиента: 3000 + 2000 = 5000₽/мес
- Единый баланс для обоих серверов
- При пополнении баланса на 10000₽ → хватит на ~60 дней для ОБОИХ серверов

### Пример 2: Изменение имени клиента

**Было:**
```
Сервер ID 5: client_name = "Не указан", client_id = 9999999
Сервер ID 6: client_name = "Не указан", client_id = 9999999
```

**Редактируете сервер ID 5:**
```
Меняете имя клиента на: "Максим Петров"
```

**Результат:**
```
Сервер ID 5: client_name = "Максим Петров", client_id = 7777777 (новый!)
Сервер ID 6: client_name = "Не указан", client_id = 9999999 (остался прежним)
```

Теперь это разные клиенты с разными балансами!

### Пример 3: Объединение серверов под одним клиентом

**Было:**
```
Сервер A: client_name = "Клиент 1", client_id = 1111
Сервер B: client_name = "Клиент 1 (новый)", client_id = 2222
```

**Редактируете сервер B:**
```
Меняете имя клиента на: "Клиент 1"  (убираете "(новый)")
```

**Результат:**
```
Сервер A: client_name = "Клиент 1", client_id = 1111
Сервер B: client_name = "Клиент 1", client_id = 1111 (объединились!)
```

Теперь у них общий баланс!

## 🔄 Алгоритм присвоения client_id

```python
def get_or_create_client_id(client_name):
    # 1. Нормализуем имя (убираем пробелы, приводим к нижнему регистру)
    normalized = client_name.strip().lower()

    # 2. Ищем существующие серверы с таким именем
    existing_servers = find_servers_by_name(normalized)

    # 3. Если нашли - берем их client_id
    if existing_servers:
        return existing_servers[0].client_id

    # 4. Если не нашли - генерируем новый ID
    # Используем MD5 хеш от имени для стабильности
    hash = md5(normalized)
    client_id = int(hash[:8], 16)  # Преобразуем в число

    return client_id
```

## ⚙️ Технические детали

### Генерация client_id

- Используется MD5 хеш от нормализованного имени клиента
- Гарантирует, что одно и то же имя всегда даст один и тот же ID
- Диапазон ID: от 1000 до 2147483647
- Коллизии обрабатываются инкрементом (+1)

### Нормализация имен

При сравнении имен система:
- Убирает лишние пробелы (`"  Иван  "` → `"Иван"`)
- Приводит к нижнему регистру (`"ИВАН"` → `"иван"`)
- Это значит: "Иван Петров", "иван петров", "  Иван Петров  " - **одно и то же**

### Автоматическое обновление балансов

При создании/редактировании сервера:
1. Присваивается client_id
2. Пересчитывается `total_monthly_cost` для клиента
3. Пересчитываются `days_remaining`
4. Обновляется или создается запись в `client_balances`

## 📝 Миграция существующих серверов

Для присвоения client_id существующим серверам выполните:

```bash
# Локально
python migrations/assign_client_ids.py

# На сервере (через Docker)
docker cp migrations/assign_client_ids.py bot-business-card:/app/migrations/
docker exec bot-business-card python /app/migrations/assign_client_ids.py
```

Скрипт:
- Найдет все серверы без client_id
- Сгруппирует их по имени клиента
- Присвоит одинаковый client_id серверам с одинаковым именем
- Создаст балансы для всех клиентов

## 🎨 В интерфейсе

После автоматического присвоения client_id:

1. **Виджет баланса появится автоматически** для всех серверов
2. **Серверы с одинаковым именем клиента** покажут одинаковый баланс
3. **При изменении имени клиента** виджет обновится автоматически
4. **Пополнение баланса** применится ко всем серверам данного клиента

## ⚠️ Важные моменты

### 1. Имя клиента = Ключ группировки

**Всегда указывайте одинаковое имя** для серверов одного клиента!

❌ Неправильно:
```
Сервер 1: "Иван"
Сервер 2: "Иван Петров"
Сервер 3: "И. Петров"
```
→ Получат разные client_id!

✅ Правильно:
```
Сервер 1: "Иван Петров"
Сервер 2: "Иван Петров"
Сервер 3: "Иван Петров"
```
→ Получат одинаковый client_id!

### 2. Изменение имени = Смена клиента

Когда вы меняете имя клиента на сервере:
- Сервер получает новый client_id
- Старый баланс остается у старого client_id
- Новый баланс создается для нового client_id

**Будьте осторожны при переименовании!**

### 3. Удаление сервера

При удалении сервера:
- client_id остается в базе
- Баланс клиента пересчитывается
- Если это был последний сервер клиента - баланс останется, но days_remaining станет 999

## 🚀 Быстрый старт

### Для нового клиента:

1. Создайте первый сервер с именем клиента (например: "ООО Ромашка")
2. client_id присвоится автоматически
3. Укажите цену для клиента (например: 2000₽/мес)
4. Нажмите кнопку "+" в столбце Баланс
5. Пополните баланс (например: 6000₽)
6. Система покажет: баланс 6000₽, дни ~90

### Для добавления сервера существующему клиенту:

1. Создайте новый сервер
2. Укажите **точно такое же** имя клиента как у других серверов
3. client_id подтянется автоматически
4. Укажите цену сервера
5. Баланс клиента автоматически пересчитается с учетом нового сервера

## 📊 Пример работы системы

```
┌─────────────────────────────────────────────────────────┐
│ Клиент: "ООО Альфа" (client_id: 12345678)              │
├─────────────────────────────────────────────────────────┤
│ Серверы:                                                │
│  • Сервер 1: CRM система        1500₽/мес              │
│  • Сервер 2: База данных        1000₽/мес              │
│  • Сервер 3: Резервный сервер    500₽/мес              │
├─────────────────────────────────────────────────────────┤
│ Общая стоимость:                3000₽/мес               │
│ Баланс:                        10000₽                   │
│ Осталось дней:                  ~100 дней               │
└─────────────────────────────────────────────────────────┘

При пополнении на 5000₽:
→ Баланс становится: 15000₽
→ Дни пересчитываются: ~150 дней
→ Изменения видны сразу на ВСЕХ трех серверах
```

## 🔧 Дополнительные возможности

### API endpoints

```python
# Получить баланс клиента
GET /admin/hosting/api/balance/{client_id}

# Пополнить баланс
POST /admin/hosting/api/balance/{client_id}/add?amount=5000&description=Пополнение

# Получить историю транзакций
GET /admin/hosting/api/balance/{client_id}/transactions?limit=50

# Получить клиентов с низким балансом
GET /admin/hosting/api/balance/low?days_threshold=5
```

### Программное использование

```python
from app.services.client_id_service import client_id_service

# Получить или создать client_id
client_id = client_id_service.get_or_create_client_id(
    db,
    client_name="ООО Ромашка",
    client_telegram_id=123456789
)

# Присвоить client_id всем серверам без него
result = client_id_service.assign_client_ids_to_all_servers(db)
```

## ❓ FAQ

**Q: Что если у меня два разных клиента с одинаковыми именами?**

A: Добавьте различающий идентификатор:
- "Иван Петров (ИП)"
- "Иван Петров (ООО)"

**Q: Можно ли вручную указать client_id?**

A: Нет, client_id присваивается автоматически на основе имени клиента. Это гарантирует консистентность.

**Q: Что делать если client_id присвоен неправильно?**

A: Измените имя клиента на нужное, система автоматически найдет или создаст правильный client_id.

**Q: Будет ли работать баланс для серверов с client_name = "Не указан"?**

A: Да! Все серверы с именем "Не указан" получат одинаковый client_id и будут иметь общий баланс. Но лучше указывать реальные имена клиентов.

**Q: Можно ли объединить балансы двух клиентов?**

A: Да! Просто переименуйте серверы одного клиента, указав имя другого клиента. Их client_id станут одинаковыми.

## 📚 См. также

- [Система балансов клиентов](./BALANCE_SYSTEM.md) (если есть)
- [API документация хостинга](./HOSTING_API.md) (если есть)
- [Интеграция с Timeweb Cloud](./TIMEWEB_INTEGRATION.md)
